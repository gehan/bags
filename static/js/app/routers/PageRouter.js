// Generated by CoffeeScript 1.3.1

define(['core/Router', 'app/views/PageView'], function(Router, PageView) {
  return new Class({
    Extends: Router,
    routes: {
      ':page/': 'page',
      ':page/:section/': 'page'
    },
    viewClass: PageView,
    page: function(args, data) {
      var pageId, section;
      pageId = args.page;
      section = args.section || 'priority';
      console.log('page', pageId, section);
      this.view.setPage(pageId);
      return this.view.getSection(section);
    }
  });
});
